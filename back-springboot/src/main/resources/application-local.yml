file:
  upload-path: C:/uploads/board/
  upload-url: /uploads/board/

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    mongodb:
      uri: mongodb://182.162.21.40:27017
      database: good_change
  datasource:
    url: jdbc:mysql://182.162.21.40:3306/good_change?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&noAccessToProcedureBodies=true
    username: goodchange
    password: Gx9#mK2$vL8@nQ4!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 200
      minimum-idle: 200
      connection-timeout: 10000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: HikariPool-GoodChange

cors:
  allowed-origins: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  metrics:
    export:
      prometheus:
        metrics:
          export:
            enabled: true
    enable:
      hikaricp: true    # ✅ 추가
      jvm: true        # ✅ 추가
      process: true    # ✅ 추가
      system: true     # ✅ 추가

logging:
  level:
    org.best: DEBUG
    org.mybatis: DEBUG