<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.mapper.MerchantMapper">

    <resultMap id="merchantResultMap" type="org.best.backspringboot.entity.Merchant">
        <id  property="merchantId"      column="merchant_id"/>
        <result property="memberId"       column="member_id"/>
        <result property="merchantName"   column="merchant_name"/>
        <result property="representative" column="representative"/>
        <result property="businessNumber" column="business_number"/>
        <result property="contact"        column="contact"/>
        <result property="address"        column="address"/>
        <result property="email"          column="email"/>
        <result property="status"         column="status"/>
        <result property="referrerId"     column="referrer_id"/>
        <result property="terminalId"     column="terminal_id"/>
        <result property="createdAt"      column="created_at"/>
        <result property="updatedAt"      column="updated_at"/>
        <result property="applyDate"   column="apply_date"/>
        <result property="approveDate" column="approve_date"/>
        <result property="loginId" column="login_id"/>  <!-- 추가 -->
        <result property="categoryName" column="category_name"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="merchantId">
        INSERT INTO merchant (
        member_id, category_id, merchant_name, representative,
        business_number, contact, contact2, address, email,
        <if test="terminalId != null and terminalId != ''">terminal_id,</if>
        status, created_at
        ) VALUES (
        #{memberId}, #{categoryId}, #{merchantName}, #{representative},
        #{businessNumber}, #{contact}, #{contact2}, #{address}, #{email},
        <if test="terminalId != null and terminalId != ''">#{terminalId},</if>
        'ACTIVE', NOW()
        )
    </insert>

    <select id="findMerchantIdByMemberId" resultType="Long">
        SELECT merchant_id FROM merchant WHERE member_id = #{memberId}
    </select>

    <select id="findCategoryNameById" resultType="String">
        SELECT category_name FROM merchant_category
        WHERE category_id = #{categoryId}
    </select>

    <select id="findCategoriesByMerchantId" resultType="string">
        SELECT category_name FROM merchant_category
        WHERE merchant_id = #{merchantId}
    </select>

    <insert id="insertCategory">
        INSERT INTO merchant_category (merchant_id, category_name, created_at)
        VALUES (#{merchantId}, #{categoryName}, NOW())
    </insert>

    <delete id="deleteCategories">
        DELETE FROM merchant_category WHERE merchant_id = #{merchantId}
    </delete>

    <select id="findAllCategories" resultType="String">
        SELECT DISTINCT category_name FROM merchant_category
        ORDER BY category_name
    </select>

    <select id="findById" resultMap="merchantResultMap">
        SELECT m.*, mb.login_id AS login_id
        FROM merchant m
                 LEFT JOIN member mb ON m.member_id = mb.member_id
        WHERE m.merchant_id = #{merchantId}
    </select>

    <select id="findByMemberId" resultMap="merchantResultMap">
        SELECT * FROM merchant WHERE member_id = #{memberId}
    </select>

    <select id="findByBusinessNumber" resultMap="merchantResultMap">
        SELECT * FROM merchant WHERE business_number = #{businessNumber}
    </select>

    <select id="findAll" resultMap="merchantResultMap">
        SELECT m.*,
        mb.login_id     AS login_id,
        mc.category_name AS category_name
        FROM merchant m
        LEFT JOIN member mb ON m.member_id = mb.member_id
        LEFT JOIN merchant_category mc ON m.category_id = mc.category_id
        <where>
            AND m.status = 'ACTIVE'
            <if test="merchantName != null and merchantName != ''">
                AND m.merchant_name LIKE CONCAT('%', #{merchantName}, '%')
            </if>
            <if test="representative != null and representative != ''">
                AND m.representative LIKE CONCAT('%', #{representative}, '%')
            </if>
            <if test="businessNumber != null and businessNumber != ''">
                AND m.business_number LIKE CONCAT('%', #{businessNumber}, '%')
            </if>
            <if test="categoryName != null and categoryName != ''">
                AND mc.category_name = #{categoryName}
            </if>
            <if test="contact != null and contact != ''">
                AND m.contact LIKE CONCAT('%', #{contact}, '%')
            </if>
            <if test="terminalId != null and terminalId != ''">
                AND m.terminal_id LIKE CONCAT('%', #{terminalId}, '%')
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                m.merchant_name    LIKE CONCAT('%', #{keyword}, '%')
                OR m.representative LIKE CONCAT('%', #{keyword}, '%')
                OR m.business_number LIKE CONCAT('%', #{keyword}, '%')
                OR mc.category_name  LIKE CONCAT('%', #{keyword}, '%')
                OR m.contact         LIKE CONCAT('%', #{keyword}, '%')
                OR m.terminal_id     LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY m.created_at DESC, m.merchant_id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM merchant WHERE status = 'ACTIVE'
    </select>

    <update id="update">
        UPDATE merchant
        <set>
            <if test="dto.merchantName   != null">merchant_name   = #{dto.merchantName},</if>
            <if test="dto.representative != null">representative  = #{dto.representative},</if>
            <if test="dto.contact        != null">contact         = #{dto.contact},</if>
            <if test="dto.address        != null">address         = #{dto.address},</if>
            <if test="dto.email          != null">email           = #{dto.email},</if>
            <if test="dto.terminalId     != null">terminal_id     = #{dto.terminalId},</if>
        </set>
        WHERE merchant_id = #{merchantId}
    </update>

    <update id="delete">
        UPDATE merchant SET status = 'DISABLED'
        WHERE merchant_id = #{merchantId}
    </update>

</mapper>