<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.member.mapper.TokenMapper">
    <insert id="upsert">
        INSERT INTO member_token (member_id, token, created_at)
        VALUES (#{memberId}, #{token}, NOW())
        ON DUPLICATE KEY UPDATE token = #{token}, created_at = NOW()
    </insert>

    <select id="findByMemberId" resultType="String">
        SELECT token FROM member_token WHERE member_id = #{memberId}
    </select>

    <delete id="deleteByMemberId">
        DELETE FROM member_token WHERE member_id = #{memberId}
    </delete>
</mapper>