<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.member.mapper.PasswordResetTokenMapper">

    <insert id="insert">
        INSERT INTO password_reset_token (member_id, token, expires_at, used, created_at)
        VALUES (#{memberId}, #{token}, #{expiresAt}, 0, NOW())
    </insert>

    <select id="findByToken" resultType="org.best.backspringboot.member.entity.PasswordResetToken">
        SELECT id, member_id, token, expires_at, used, created_at
        FROM password_reset_token
        WHERE token = #{token}
    </select>

    <update id="markUsed">
        UPDATE password_reset_token SET used = 1 WHERE token = #{token}
    </update>

    <delete id="deleteByMemberId">
        DELETE FROM password_reset_token WHERE member_id = #{memberId}
    </delete>
</mapper>