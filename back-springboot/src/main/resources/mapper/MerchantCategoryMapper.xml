<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.merchant.mapper.MerchantCategoryMapper">

    <resultMap id="categoryResultMap" type="org.best.backspringboot.merchant.entity.MerchantCategory">
        <id     property="categoryId"   column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="description"  column="description"/>
        <result property="createdAt"    column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="categoryResultMap">
        SELECT * FROM merchant_category ORDER BY category_id ASC
    </select>

    <select id="findById" resultMap="categoryResultMap">
        SELECT * FROM merchant_category WHERE category_id = #{categoryId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="categoryId">
        INSERT INTO merchant_category (category_name, description, created_at)
        VALUES (#{categoryName}, #{description}, NOW())
    </insert>

    <update id="update">
        UPDATE merchant_category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="description  != null">description   = #{description},</if>
        </set>
        WHERE category_id = #{categoryId}
    </update>

    <delete id="delete">
        DELETE FROM merchant_category WHERE category_id = #{categoryId}
    </delete>

</mapper>
