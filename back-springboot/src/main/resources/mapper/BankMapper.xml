<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.mapper.BankMapper">

    <resultMap id="bankResultMap" type="org.best.backspringboot.entity.Bank">
        <id  property="bankId"    column="bank_id"/>
        <result property="bankName"   column="bank_name"/>
        <result property="bankCode"   column="bank_code"/>
        <result property="status"     column="status"/>
        <result property="createdAt"  column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="bankResultMap">
        SELECT * FROM bank
        WHERE status = 'ACTIVE'
        ORDER BY bank_id ASC
    </select>

    <select id="findById" resultMap="bankResultMap">
        SELECT * FROM bank WHERE bank_id = #{bankId}
    </select>

</mapper>
