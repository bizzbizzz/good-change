<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.best.backspringboot.mapper.MemberMapper">
    <resultMap id="memberResultMap" type="org.best.backspringboot.entity.Member">
        <id property="memberId"   column="member_id"/>
        <result property="loginId"    column="login_id"/>
        <result property="roleId"     column="role_id"/>
        <result property="password"   column="password"/>
        <result property="name"       column="name"/>
        <result property="birthDate"  column="birth_date"/>
        <result property="gender"     column="gender"/>
        <result property="address"    column="address"/>
        <result property="email"      column="email"/>
        <result property="referrerId" column="referrer_id"/>
        <result property="point"      column="point"/>
        <result property="status"     column="status"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
        <result property="organization"  column="organization"/>
        <result property="applyDate"   column="apply_date"/>
        <result property="approveDate" column="approve_date"/>
        <collection property="cardNumbers" ofType="String"
                    select="findCardNumbersByMemberId" column="member_id"/>
    </resultMap>

    <select id="findCardNumbersByMemberId" resultType="String">
        SELECT card_number FROM card
        WHERE member_id = #{memberId}
          AND status != 'DELETED'
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (
            role_id, login_id, password, name, birth_date,
            gender, address, organization, email, point, status,
            referrer_id, apply_date, approve_date, created_at
        ) VALUES (
                     #{roleId}, #{loginId}, #{password}, #{name}, #{birthDate},
                     #{gender}, #{address}, #{organization}, #{email}, 0, 'ACTIVE',
                     #{referrerId}, #{applyDate}, #{approveDate}, NOW()
                 )
    </insert>

    <select id="findRoleNameById" resultType="String">
        SELECT role_name FROM role WHERE role_id = #{roleId}
    </select>

    <update id="updateStatus">
        UPDATE member SET status = #{status} WHERE login_id = #{loginId}
    </update>

    <select id="findById" resultMap="memberResultMap">
        SELECT * FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="findAll" resultMap="memberResultMap">
        SELECT m.* FROM member m
        LEFT JOIN card c ON c.member_id = m.member_id
        WHERE m.role_id = 2
        <if test="name != null and name != ''">
            AND m.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="birthDate != null and birthDate != ''">
            AND DATE_FORMAT(m.birth_date, '%Y-%m-%d') LIKE CONCAT('%', #{birthDate}, '%')
        </if>
        <if test="cardNumber != null and cardNumber != ''">
            AND c.card_number LIKE CONCAT('%', #{cardNumber}, '%')
        </if>
        <if test="organization != null and organization != ''">
            AND m.organization LIKE CONCAT('%', #{organization}, '%')
        </if>
        <if test="status != null and status != ''">
            AND m.status = #{status}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
            m.name LIKE CONCAT('%', #{keyword}, '%')
            OR DATE_FORMAT(m.birth_date, '%Y-%m-%d') LIKE CONCAT('%', #{keyword}, '%')
            OR c.card_number LIKE CONCAT('%', #{keyword}, '%')
            OR m.organization LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        GROUP BY m.member_id
        ORDER BY m.created_at DESC, m.member_id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(DISTINCT m.member_id) FROM member m
        LEFT JOIN card c ON c.member_id = m.member_id
        WHERE m.role_id = 2
        <if test="name != null and name != ''">
            AND m.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="birthDate != null and birthDate != ''">
            AND DATE_FORMAT(m.birth_date, '%Y-%m-%d') LIKE CONCAT('%', #{birthDate}, '%')
        </if>
        <if test="cardNumber != null and cardNumber != ''">
            AND c.card_number LIKE CONCAT('%', #{cardNumber}, '%')
        </if>
        <if test="status != null and status != ''">
            AND m.status = #{status}
        </if>
        <if test="organization != null and organization != ''">
            AND m.organization LIKE CONCAT('%', #{organization}, '%')
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
            m.name LIKE CONCAT('%', #{keyword}, '%')
            OR DATE_FORMAT(m.birth_date, '%Y-%m-%d') LIKE CONCAT('%', #{keyword}, '%')
            OR c.card_number LIKE CONCAT('%', #{keyword}, '%')
            OR m.organization LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    </select>

    <select id="findLeaveList" resultMap="memberResultMap">
        SELECT * FROM member
        WHERE status = 'DISABLED'
        ORDER BY updated_at DESC
            LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countLeave" resultType="long">
        SELECT COUNT(*) FROM member
        WHERE status = 'DISABLED'
    </select>

    <update id="updatePoint">
        UPDATE member SET point = #{point} WHERE member_id = #{memberId}
    </update>

    <select id="findByLoginId" resultMap="memberResultMap">
        SELECT * FROM member
        WHERE login_id = #{loginId}
    </select>

    <update id="update">
        UPDATE member
        <set>
            <if test="dto.name    != null">name    = #{dto.name},</if>
            <if test="dto.address != null">address = #{dto.address},</if>
            <if test="dto.email   != null">email   = #{dto.email},</if>
        </set>
        WHERE login_id = #{loginId}
    </update>

    <update id="delete">
        UPDATE member
        SET status = 'DISABLED'
        WHERE login_id = #{loginId}
    </update>

</mapper>